version: '3.2'
services:
  app:
    build:
      context: ../..
      dockerfile: ./Dockerfile
      args: 
        - VAR=test
    environment:
      - ENV_VARIABLE=test
    # links:
    #   - db
    #   - uaa